import { createClient } from '@/lib/supabase/server'; import { requireUserId } from '@/lib/security'; import { jsonError, jsonResult, readJson } from '@/lib/api'; import { runDealAnalysis } from '@/modules/deal-intel/service'; import type { AnalyzeDealInput } from '@/modules/deal-intel/schemas';
export const dynamic = 'force-dynamic';
export async function POST(request: Request, { params }: { params: { dealId: string } }) { const supabase = createClient(); const auth = await requireUserId(supabase); if (!auth.ok) return jsonError(auth.error); return jsonResult(await runDealAnalysis(supabase, params.dealId, (await readJson(request)) as AnalyzeDealInput), 202); }
